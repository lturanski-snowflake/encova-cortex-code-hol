# Module 0.1 — Set Context

Run this SQL directly (replace `<NAME>` with your assigned first name from the attendees list):

```sql
USE ROLE ATTENDEE_ROLE;
USE DATABASE TRAINING_<NAME>;
USE SCHEMA BRONZE;
USE WAREHOUSE ENCOVA_TRAINING_WH;
```

> **Note:** Your database is `TRAINING_<NAME>`. You have three schemas: `BRONZE`, `SILVER`, `GOLD`. Raw source data lives in the shared `ENCOVA_TRAINING.RAW_DATA` database. The warehouse is shared across all attendees (multi-cluster, auto-scales).
